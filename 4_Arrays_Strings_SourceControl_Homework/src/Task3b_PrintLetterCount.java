import java.util.Scanner;

public class Task3b_PrintLetterCount {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        String inputString = sc.nextLine();
        String result = inputString.toLowerCase();
        Integer countA = result.length() - result.replaceAll("a", "").length();
        if (countA > 0) {
            System.out.printf("a(%d)\n", countA);
        }
        Integer countB = result.length() - result.replaceAll("b", "").length();
        if (countB > 0) {
            System.out.printf("b(%d)\n", countB);
        }
        Integer countC = result.length() - result.replaceAll("c", "").length();
        if (countC > 0) {
            System.out.printf("c(%d)\n", countC);
        }
        Integer countD = result.length() - result.replaceAll("d", "").length();
        if (countD > 0) {
            System.out.printf("d(%d)\n", countD);
        }
        Integer countE = result.length() - result.replaceAll("e", "").length();
        if (countE > 0) {
            System.out.printf("e(%d)\n", countE);
        }
        Integer countF = result.length() - result.replaceAll("f", "").length();
        if (countF > 0) {
            System.out.printf("f(%d)\n", countF);
        }
        Integer countG = result.length() - result.replaceAll("g", "").length();
        if (countG > 0) {
            System.out.printf("g(%d)\n", countG);
        }
        Integer countH = result.length() - result.replaceAll("h", "").length();
        if (countH > 0) {
            System.out.printf("h(%d)\n", countH);
        }
        Integer countI = result.length() - result.replaceAll("i", "").length();
        if (countI > 0) {
            System.out.printf("i(%d)\n", countI);
        }
        Integer countJ = result.length() - result.replaceAll("j", "").length();
        if (countJ > 0) {
            System.out.printf("j(%d)\n", countJ);
        }
        Integer countK = result.length() - result.replaceAll("k", "").length();
        if (countK > 0) {
            System.out.printf("k(%d)\n", countK);
        }
        Integer countL = result.length() - result.replaceAll("l", "").length();
        if (countL > 0) {
            System.out.printf("l(%d)\n", countL);
        }
        Integer countM = result.length() - result.replaceAll("m", "").length();
        if (countM > 0) {
            System.out.printf("m(%d)\n", countM);
        }
        Integer countN = result.length() - result.replaceAll("n", "").length();
        if (countN > 0) {
            System.out.printf("n(%d)\n", countN);
        }
        Integer countO = result.length() - result.replaceAll("o", "").length();
        if (countO > 0) {
            System.out.printf("o(%d)\n", countO);
        }
        Integer countP = result.length() - result.replaceAll("p", "").length();
        if (countP > 0) {
            System.out.printf("p(%d)\n", countP);
        }
        Integer countQ = result.length() - result.replaceAll("q", "").length();
        if (countQ > 0) {
            System.out.printf("q(%d)\n", countQ);
        }
        Integer countR = result.length() - result.replaceAll("r", "").length();
        if (countR > 0) {
            System.out.printf("r(%d)\n", countR);
        }
        Integer countS = result.length() - result.replaceAll("s", "").length();
        if (countS > 0) {
            System.out.printf("s(%d)\n", countS);
        }
        Integer countT = result.length() - result.replaceAll("t", "").length();
        if (countT > 0) {
            System.out.printf("t(%d)\n", countT);
        }
        Integer countU = result.length() - result.replaceAll("u", "").length();
        if (countU > 0) {
            System.out.printf("u(%d)\n", countU);
        }
        Integer countV = result.length() - result.replaceAll("v", "").length();
        if (countV > 0) {
            System.out.printf("v(%d)\n", countV);
        }
        Integer countW = result.length() - result.replaceAll("w", "").length();
        if (countW > 0) {
            System.out.printf("w(%d)\n", countW);
        }
        Integer countX = result.length() - result.replaceAll("x", "").length();
        if (countX > 0) {
            System.out.printf("x(%d)\n", countX);
        }
        Integer countY = result.length() - result.replaceAll("y", "").length();
        if (countY > 0) {
            System.out.printf("y(%d)\n", countY);
        }
        Integer countZ = result.length() - result.replaceAll("z", "").length();
        if (countZ > 0) {
            System.out.printf("z(%d)\n", countZ);
        }
    }
}
